jar {
    manifest {
        attributes(
                'Main-Class': 'com.mogikanlol.game.desktop.DesktopLauncher'
        )
    }
    from {
        configurations.runtimeClasspath.collect { it.isDirectory() ? it : zipTree(it) }
    }
}